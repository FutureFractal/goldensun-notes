---
context_type: issue
status: todo
---

Parent: [[lan/task/000 setup Coaltergeist decomp proj/000 setup Coaltergeist decomp proj]]

Spawned by: [[000 Issues encountered building camelot-gcc]]

Spawned in: [[000 Issues encountered building camelot-gcc#^spawn-issue-d1302a|^spawn-issue-d1302a]]

# Prior Logs leading to error

2026-05-25 Wk 22 Mon - 08:01 +03:00

My system is `Ubuntu 25.04`. Let's get to setting this up.

```sh
mkdir -p ~/src/cloned/gh/Coaltergeist
cd ~/src/cloned/gh/Coaltergeist
git clone git@github.com:Coaltergeist/goldensun-decomp.git
```

According to the README, we need a `baserom.gba` for `Golden Sun (2001)` matching the following checksum:

> * **goldensun.gba** `sha1: 5c4695205413df7db52b9a184815a07783999971` (USA)

2026-05-25 Wk 22 Mon - 08:12 +03:00

```sh
# in /home/lan/src/cloned/gh/Coaltergeist/goldensun-decomp
sha1sum baserom.gba

# out
5c4695205413df7db52b9a184815a07783999971  baserom.gba
```

We have it.

Following [INSTALL.md](https://github.com/Coaltergeist/goldensun-decomp/blob/main/INSTALL.md),

```sh
sudo apt update
sudo apt install build-essential binutils-arm-none-eabi python3 git \
                 bison flex texinfo
```

```sh
cd ~/src/cloned/gh/Coaltergeist
git clone https://github.com/Coaltergeist/camelot-gcc
cd camelot-gcc
chmod +x ./build-296.sh ./install-296.sh
./build-296.sh
```

Ran into an error:

```sh
[4/4] make cc1 + xgcc + cpp + tradcpp
cd /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc; autoconf
/usr/bin/m4:configure.in:418: recursion limit of 1024 exceeded, use -L<N> to change it
autom4te: error: /usr/bin/m4 failed with exit status: 1
make: *** [Makefile:946: /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc/configure] Error 1
```

# Journal

2026-05-25 Wk 22 Mon - 08:50 +03:00

To reproduce,

```sh
# in /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/build-296/gcc
CFLAGS="-O2 -fno-pie -no-pie -Wno-narrowing -Wno-implicit-int -Wno-implicit-function-declaration -Wno-pointer-arith -Wno-int-conversion -Wno-format -Wno-error -fcommon" CXXFLAGS="-O2 -fno-pie -no-pie -Wno-narrowing -Wno-error" LDFLAGS="-no-pie" make cc1
```

I can control the recursion limit in a sketchy way by temporarily moving `/usr/bin/m4/` to `/home/$USER/tmp/restore/m4` and then:

```sh
# in file /usr/bin/m4
#!/bin/sh

/home/$USER/tmp/restore/m4 $@ -L0
```

But this leads to a stack overflow:

```sh
cd /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc; autoconf
m4: stack overflow
autom4te: error: /usr/bin/m4 failed with exit status: 1
make: *** [Makefile:946: /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc/configure] Error 1
```

So it seems an infinite recursion might be happening for some reason.

-- 2026-05-25 Wk 22 Mon - 09:29 +03:00

To get the `configure.in` used ~~(seems autogenerated)~~:

```sh
# in file /usr/bin/m4
#!/bin/sh

cat configure.in >> /tmp/a
/home/$USER/tmp/restore/m4 $@ -L0
```

---- 2026-05-25 Wk 22 Mon - 09:42 +03:00

Turns out it was from `/home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/build-296/gcc
`.

2026-05-25 Wk 22 Mon - 09:25 +03:00

`configure.in:418`, line 418 is `gcc_AC_CHECK_PROG_VER(MAKEINFO, makeinfo, --version,`:

```
gcc_AC_CHECK_PROG_VER(MAKEINFO, makeinfo, --version,
[GNU texinfo.* \([0-9][0-9.]*\)],
[3.1[2-9] | 3.[2-9][0-9] | 4.* | 1.6[89] | 1.7[0-9]])
```

I have:

```sh
makeinfo --version

# out
texi2any (GNU texinfo) 7.1.1
```

2026-05-25 Wk 22 Mon - 09:39 +03:00

```
CFLAGS="-O2 -fno-pie -no-pie -Wno-narrowing -Wno-implicit-int -Wno-implicit-function-declaration -Wno-pointer-arith -Wno-int-conversion -Wno-format -Wno-error -fcommon" CXXFLAGS="-O2 -fno-pie -no-pie -Wno-narrowing -Wno-error" LDFLAGS="-no-pie" make cc1
```

```sh
#!/bin/sh

echo $(pwd) > /tmp/a
/home/$USER/tmp/restore/m4 $@ -L0
```

This log points to `/home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc` even though we initiated the command from `/home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/build-296/gcc
`.

```sh
# in /home/lan/src/cloned/gh/Coaltergeist/camelot-gcc/gcc-2.96/gcc
cp configure.in configure.in.backup
```

Removing the section 

```
# Do we have a single-tree copy of texinfo?
if test -f $srcdir/../texinfo/Makefile.in; then
  MAKEINFO='$(objdir)/../texinfo/makeinfo/makeinfo'
  gcc_cv_prog_makeinfo_modern=yes
  AC_MSG_RESULT([Using makeinfo from the unified source tree.])
else
  # See if makeinfo has been installed and is modern enough
  # that we can use it.
  gcc_AC_CHECK_PROG_VER(MAKEINFO, makeinfo, --version,
  [GNU texinfo.* \([0-9][0-9.]*\)],
  [3.1[2-9] | 3.[2-9][0-9] | 4.* | 1.6[89] | 1.7[0-9] | 7.1.1])
fi
```

from `configure.in` allows it to continue and not run into a stack overflow, and then we hit this:

```
Configuration x86_64-unknown-linux-gnu not supported
make: *** [Makefile:976: config.status] Error 1
```

corresponding to this in `configure.in`:

```
echo "Configuration $machine not supported" 1>&2
exit 1
```

2026-05-25 Wk 22 Mon - 10:06 +03:00

Maybe I can try this on a fresh system.

```sh
TAG="ubuntu_25.04" MOUNT="." sh <(curl -sSf https://raw.githubusercontent.com/delta-domain-rnd/delta-box/refs/heads/main/box/box000_blank_system/docker_root_sh.sh)

# in container
cd ~
apt-get update
apt install build-essential binutils-arm-none-eabi python3 git \
                 bison flex texinfo
git clone https://github.com/Coaltergeist/camelot-gcc

# Some extra dependencies that the build ended up needing:
apt install autoconf

cd camelot-gcc
chmod +x ./build-296.sh ./install-296.sh
./build-296.sh
```

The issue is reproduced:

```
[4/4] make cc1 + xgcc + cpp + tradcpp
cd /root/camelot-gcc/gcc-2.96/gcc; autoconf
/usr/bin/m4:configure.in:418: recursion limit of 1024 exceeded, use -L<N> to change it
autom4te: error: /usr/bin/m4 failed with exit status: 1
make: *** [Makefile:946: /root/camelot-gcc/gcc-2.96/gcc/configure] Error 1
```
